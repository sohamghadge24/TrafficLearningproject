# Core dependencies
torch>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
tqdm>=4.65.0
tensorboard>=2.13.0

# Optional: SUMO (install separately)
# traci - comes with SUMO installation
# sumolib - comes with SUMO installation

# Development dependencies (optional)
jupyter>=1.0.0
ipython>=8.12.0